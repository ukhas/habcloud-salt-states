{% set hostname = "habhub.org" %}
{% set aliases = ["www.habhub.org"] %}
{% set default_host = true %}
{% extends "nginx/site-template.conf" %}

{% block server %}
    index index.html;
 
    location = /calc { return 301 /calc/; }
    location /calc/ {
        alias /srv/cusf-burst-calc/;
    }

    location /files/ {
        proxy_pass http://shells.private.habhub.org/files/;
    }

    location /kml_testing/ {
    }

    location /notam_overlay/ {
    }

    location /zeusbot/ {
        proxy_pass http://daemons.private.habhub.org/zeusbot/;
    }

    location / {
        alias /srv/habhub-homepage/;
    }
{% endblock %}
